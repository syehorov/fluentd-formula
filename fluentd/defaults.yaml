# -*- coding: utf-8 -*-
# vim: ft=yaml
---
fluentd:
  install_packages: true
  config:
    base: /etc/td-agent
    filename: td-agent.conf
  service: td-agent
  forward:
    enabled: False
  unix:
    enabled: False
  http:
    enabled: False
  debug_agent:
    enabled: False
  